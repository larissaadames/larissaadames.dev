const downloadCVButton = document.getElementById('download-cv');
const contactMeButton = document.getElementById('contact-me');

downloadCVButton.addEventListener('click', () => {
    // Adicione aqui a lógica para baixar o CV, por exemplo:
    // window.open('seu-cv.pdf', '_blank'); 
});

contactMeButton.addEventListener('click', () => {
    // Adicione aqui a lógica para abrir um formulário de contato, por exemplo:
    // window.location.href = 'contato.html';
});